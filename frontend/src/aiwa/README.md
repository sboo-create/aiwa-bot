# @aiwa/ui (local package)

Reusable AIWA product components built on Deslop TMA.

```js
import { HomeScreen, PaperRow, AiwaModalView, Field, installBridge } from "./aiwa";
```

Entry used by Vite: `src/deslop-main.jsx` installs `window.AiwaDeslop`.

See `AGENTS.md` for agent rules.
